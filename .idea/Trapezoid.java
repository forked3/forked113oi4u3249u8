public class Trapezoid extends Shape{
    int topLength;
    int baseLength;

    public Trapizoid(int topLength, int baseLength) {
        this.topLength = topLength;
        this.baseLength = baseLength;
    }
}