Public class Square extends Shape{
    int sideLength;

    public Square(int x, int y, int sideLength) {
        super(x,y);
        this.x = x;
        this.y = y;
        this.sideLength = sideLength;
    }
}